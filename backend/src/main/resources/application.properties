spring.application.name=demo

spring.datasource.url=jdbc:postgresql://postgresforpro.cw52wo446izi.us-east-1.rds.amazonaws.com:5432/postgres
spring.datasource.username=postgres_ForPro
spring.datasource.password=Promech0980

spring.jpa.hibernate.ddl-auto=update

server.address=0.0.0.0

#port 8081 is to connect to spring  boot server(8081 is also the port of flutter app and postman)     5432 is port of postgresql database
server.port=8081           




spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.web.cors.allowed-origin-patterns=*

# Allow larger uploads (banners, posters, etc.) – 413 = increase if needed
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=25MB

# AWS S3 (required for banners, posters, vehicle catalog – uploads go to S3, app shows images from S3 URLs)
# On EC2: copy application-ec2.properties.template to backend-app/application-ec2.properties and set your bucket + IAM credentials.
aws.s3.bucket=
aws.s3.region=us-east-1
aws.access-key-id=
aws.secret-access-key=

# Square Payment Gateway Configuration (Sandbox)
square.environment=sandbox
square.application.id=sandbox-sq0idb-0UJaOZfiilnERiwDLf9t_Q
square.access.token=EAAAlxcrAOyG7VOxzEvHoOPXD91DqpuYLhnh4TuRRr6u47bBn52r6lpP1L9oO0mt
# Location ID is optional - will be auto-fetched if available in sandbox

# Dashboard (web admin). Do NOT commit real secrets; use env vars on EC2.
dashboard.jwt.secret=${DASHBOARD_JWT_SECRET:dyganox-dashboard-secret-key-min-32-chars-long}
dashboard.owner.password=${DASHBOARD_OWNER_PASSWORD:}
dashboard.access.code=${DASHBOARD_ACCESS_CODE:}
# SES for OTP (optional)
aws.ses.from=${AWS_SES_FROM:}
aws.ses.region=${AWS_SES_REGION:us-east-1}

# App (Flutter) customer pool – for login-with-OTP. Same pool as in lib/services/cognito_service.dart.
# If app client has no secret, leave app.cognito.client-secret empty.
app.cognito.region=${APP_COGNITO_REGION:us-east-1}
app.cognito.pool-id=${APP_COGNITO_POOL_ID:us-east-1_vXPHD9qbi}
app.cognito.client-id=${APP_COGNITO_CLIENT_ID:7o5vr364ksd1vbduhm05ea7odq}
app.cognito.client-secret=${APP_COGNITO_CLIENT_SECRET:}

# FCM (optional): Legacy server key from Firebase Console → Project Settings → Cloud Messaging. When set, mechanics receive push when user sends request.
# fcm.server.key=YOUR_LEGACY_SERVER_KEY